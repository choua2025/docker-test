services:
  app:
    build: .
    image: docker-project
    container_name: docker-project
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
    restart: unless-stopped
