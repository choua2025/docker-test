services:
  app:
    build: .
    image: docker-project
    container_name: docker-project
    ports:
      - "6000:6000"
    environment:
      NODE_ENV: production
      PORT: 6000
    restart: unless-stopped
